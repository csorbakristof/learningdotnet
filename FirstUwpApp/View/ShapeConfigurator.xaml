<UserControl
    x:Class="FirstUwpApp.View.ShapeConfigurator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FirstUwpApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <RelativePanel>
            <Slider x:Name="WidthSlider" Margin="0,0,0,0" Header="Width" Value="10" Minimum="10" Maximum="100"/>
            <Slider x:Name="HeightSlider" Orientation="Vertical" Margin="0,20,0,0" Header="Height" Value="10" Minimum="10" Maximum="100"
                    RelativePanel.Below="WidthSlider"
                    RelativePanel.AlignBottomWithPanel="True"/>
            <ColorPicker x:Name="ColorPicker" Margin="20,0,0,0" Color="#CD5CFF"
                    RelativePanel.RightOf="HeightSlider"
                    RelativePanel.AlignTopWithPanel="True"
                    RelativePanel.AlignBottomWithPanel="True"/>
        </RelativePanel>
    </Grid>
</UserControl>
