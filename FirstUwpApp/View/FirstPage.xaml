<Page
    x:Class="FirstUwpApp.View.FirstPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FirstUwpApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Buttons.(RelativePanel.RightOf)" Value="ShapeType"/>
                        <Setter Target="Buttons.(RelativePanel.Below)" Value=""/>
                        <Setter Target="Colors.(RelativePanel.RightOf)" Value="Buttons"/>
                        <Setter Target="Colors.(RelativePanel.Below)" Value=""/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <RelativePanel>
            <StackPanel x:Name="ShapeType" Margin="20">
                <ComboBox x:Name="Type" Header="Type of shape" Margin="0,24,0,0" PlaceholderText="shape type">
                    <ComboBoxItem Name="circle">Circle</ComboBoxItem>
                    <ComboBoxItem Name="polygon">Polygon</ComboBoxItem>
                </ComboBox>
                <TextBox x:Name="Title" Header="Shape title" Margin="0,24,0,0" HorizontalAlignment="Left" />
            </StackPanel>
            <StackPanel x:Name="Colors" Margin="20" RelativePanel.Below="ShapeType">
                <ColorPicker x:Name="Color" Margin="0,24,0,0"/>
            </StackPanel>
            <StackPanel x:Name="Buttons" Orientation="Horizontal" RelativePanel.Below="Colors">
                <Button x:Name="CreateShapeButton" Content="Create shape" Margin="24" Click="CreateShapeButton_Click"/>
            </StackPanel>
            <Canvas x:Name="Canvas" MinWidth="100" MinHeight="100" Background="Gray" RelativePanel.Below="ShapeType" />
        </RelativePanel>
    </Grid>
</Page>
